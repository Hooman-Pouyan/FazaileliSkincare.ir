# If the public registry is unreachable from an Iranian IP, point at a mirror:
#   registry=https://registry.npmmirror.com/
# Keep the lockfile committed and BUILD THE DOCKER IMAGE, don't build on the
# server — see docs/02-adr-002-infrastructure.md.
auto-install-peers=true
strict-peer-dependencies=false
