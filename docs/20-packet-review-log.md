# Packet review log

**Purpose:** every packet closes with the judgements that deserve a second look —
calls made on thin evidence, deliberate omissions, product questions raised by
building, and refinements not worth blocking on. Nothing here is a defect; open
defects go in the ledger.

Read this when a packet is revisited for polish, or when deciding whether a
carried gap has become a real problem.

**Status key:** `open` needs a decision · `carried` is deliberate and dormant ·
`resolved` has been settled, with the outcome recorded.

---

## Packet 1 — execution ledger and status corrections

| #   | What                                                                                                                                                                                               | Kind      | Status  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------- |
| 1.1 | Five numbering schemes still coexist. The ledger indexes them but does not replace them; if the project ever feels heavy to navigate, collapsing DB/AUTH/Stage IDs into one is the change to make. | structure | carried |

## Packet 2 — fictional development catalogue

| #   | What                                                                                                                                                                                                                           | Kind      | Status  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | ------- |
| 2.1 | Ten products across five concerns is enough to exercise every offer state but not enough to judge a grid, a facet rail, or pagination. When the PLP lands, decide whether to grow the fixture or wait for real catalogue data. | product   | open    |
| 2.2 | Placeholder imagery is flat colour with a number. It proves layout, not art direction; the hub and PDP cannot be judged aesthetically against it.                                                                              | UI        | carried |
| 2.3 | Stock is written directly rather than through an `inventory_movement`. Correct for fixture data, but it means the movement ledger is untested until DB5.                                                                       | technical | carried |

## Packet 3 — catalogue reads

| #   | What                                                                                                                                                                                                                        | Kind            | Status  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------- |
| 3.1 | **Sort default is `featured`** (curated `merchandising_rank`). Chosen because relevance is meaningless outside search and newest reorders a small catalogue on every import — but nobody has seen it against real products. | product         | open    |
| 3.2 | **Price bounds cross the URL in toman, storage is rials.** One conversion at the query boundary. Worth confirming this reads naturally to a Persian shopper before URLs are indexed.                                        | product         | open    |
| 3.3 | Facet counts are not implemented. `PLP-03` needs each group counted with its own selections removed — a query per group — which lands with the rail that renders them in packet 6.                                          | technical       | carried |
| 3.4 | The trigram index will sit unused until roughly a thousand products. Expected, evidenced in `evidence/c3-trgm-search.md`, and the re-check gate is named there.                                                             | technical       | carried |
| 3.5 | **Media resolution uses `cardObjectKey`/`detailObjectKey` verbatim.** Fine for local paths; when object storage exists this becomes a CDN base plus key, and the decision of where that base lives is unmade.               | technical       | open    |
| 3.6 | Professional-only products are visible and non-purchasable (D-18-2). Whether Iranian rules or a brand agreement restrict advertising professional _pricing_ publicly is still the owner's to confirm.                       | product / legal | open    |

## Packet 4 — the storefront shell

### Worth your attention

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Kind            | Status        |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------- |
| 4.1  | **The bottom bar carries four items and excludes Booking and Academy.** Reasoned from the IA — a bottom bar is repeat-navigation furniture — not measured. If either turns out to be a repeat destination, this is wrong and cheap to change.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | product         | open          |
| 4.2  | **The footer omits the address and telephone.** The storefront canvas marks both `[براکت]`, and `SHELL-05` forbids inventing contact detail. They appear the moment you supply them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | content         | **needs you** |
| 4.3  | **Terms, privacy and returns are linked but unwritten**, so three footer links 404 today. `SHELL-05` requires them visible and eNamad will not certify the domain without them. This is the longest-lead item in the packet and it is not an engineering task.                                                                                                                                                                                                                                                                                                                                                                                                                                           | content / legal | **needs you** |
| 4.4  | **The eNamad slot is an empty bordered square.** It holds layout space so nothing shifts when the real seal arrives. Decide whether an empty slot is acceptable pre-certification or whether it should render nothing at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | UI              | open          |
| 4.5  | **The command palette lists rooms, not concerns.** Decision N-4 says it should list the five canonical concerns; they live in the database and reading them in the shell would put a query on every page including the landing. Arrives with a cached reference read.                                                                                                                                                                                                                                                                                                                                                                                                                                    | technical       | carried       |
| 4.6  | **`/account` is the identity destination and `/studio` stays planned.** Revisit when Studio is built in Phase 5: does it absorb the account page or sit beside it in the rail?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | product         | carried       |
| 4.7  | **Locale switching keeps the path and shows `locale-unavailable` when content is missing.** Untested against real English or Arabic content, because none exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | product         | carried       |
| 4.8  | **Bodoni Moda ships as variable TrueType, not woff2.** The declaration matches the files, so display type renders — but woff2 roughly halves both files, and over Iranian mobile that is worth having. Needs `fonttools` with brotli, which neither of this session's environments could install.                                                                                                                                                                                                                                                                                                                                                                                                        | performance     | open          |
| 4.9  | **`pnpm dlx shadcn@latest add command` overwrote the customised `dialog.tsx`** — replacing token bindings with literal `bg-black/50`, adding `shadow-lg` against the hairlines-not-shadows rule, swapping the two project radii for Tailwind's ramp, and reintroducing physical `left`/`right`. It also renamed a prop out from under `search-command.tsx`, which typecheck caught. All re-bound; the workflow rule is now in AGENTS.md.                                                                                                                                                                                                                                                                 | process         | resolved      |
| 4.10 | **No skeleton on `loading.tsx`.** A skeleton that guesses at a layout it does not know promises a shape the real page may not have. Revisit per route once the real layouts exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | UI              | carried       |
| 4.12 | **Nineteen class names in eight files compiled to nothing.** `inset-inline-*`, `inset-block-*` and `border-inline-end` are CSS property names, not Tailwind utilities. The desktop rail and the mobile bottom bar were `fixed` with no offsets and only looked right because static flow happened to place them there; the sheet, dialog, product tile, facet rail, cart drawer and concern rail were all affected. The ESLint message was itself recommending `inset-inline-*`. Fixed, the rule corrected, and `src/lib/design/tailwind-candidates.test.ts` added as the general gate — it compiles every class name in `src/` against this project's real theme and fails on any that produces no CSS. | verification    | resolved      |
| 4.13 | **`command.tsx` and `dialog.tsx` referenced four shadcn palette names this project never defined** — `popover`, `popover-foreground`, `accent`, `accent-foreground` — so the command palette had no background of its own. Re-bound to project tokens rather than widened into the compatibility bridge in `globals.css`, which would grow a second palette vocabulary. Same root cause as 4.9: a CLI-added component was not diffed against the token layer.                                                                                                                                                                                                                                            | drift           | resolved      |
| 4.14 | **Two spellings for the same token are in use** — `bg-[var(--surface)]` alongside the bound `bg-surface`, and arbitrary type sizes (`text-[19px]`) alongside the scale (`text-h3`). Both reference tokens so neither is a raw hex violation, but it is two mechanisms for one concern. New code uses the bound utilities; normalising what exists is a mechanical sweep worth doing in one pass rather than mid-packet.                                                                                                                                                                                                                                                                                  | drift           | open          |
| 4.11 | **The shell has had its browser pass.** Rail, footer and bottom navigation were exercised at 390, 768 and 1440 in Persian across six routes. The rail sits on the correct edge in RTL, no screen re-adds `ms-14` over the shell's own offset, there is no horizontal overflow at any width and no console errors on any route. One defect came out of it and is fixed: eight footer links were under the WCAG 2.2 AA target-size floor — see `6c.3`.                                                                                                                                                                                                                                                     | verification    | resolved      |

### Refinements not worth blocking on

- The rail's brand medallion is a bordered circle with a lapis dot, carried from the existing scaffold. The real mark, when there is one, replaces it in one place.
- Room accent colours come from `--accent-*` tokens, so changing the active-state colour is a token edit, not a component edit.
- `BottomNavigation` and `Rail` share the manifest but not markup. If a third surface appears, extract the shared item shape then — not before.

---

## Packet 5 — the Shop hub

| #   | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Kind         | Status        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------- |
| 5.1 | **Seventeen scaffold components were deleted, not migrated.** `src/components/commerce/` held a parallel component library that nothing imported, built on its own `ProductSummary` view model — a second shape for a product, with its own price formatting and its own hardcoded `/shop/p/{slug}` URLs. Keeping it meant keeping the duplication the page models exist to remove. The five the hub replaced are rebuilt against the real models; the twelve for PLP, PDP and cart are recoverable from commit `0c4040c` and are rebuilt when their page models exist. The design thinking in them is already written down in `docs/10-design-playbook.md` and the PLP/PDP plans. | structure    | resolved      |
| 5.2 | **The hub's metadata copy now comes from the read, and the route caches the read.** `getShopHub` had a hardcoded Persian `meta.title`; the alternative was translating in the route, which would have been a second place for the hub's name to live. `generateMetadata` and the page share one query through React's request cache. Worth a look when the PLP lands, because its meta genuinely varies with the query and the same pattern has to hold.                                                                                                                                                                                                                           | technical    | open          |
| 5.3 | **`enquiryHref` is `https://wa.me/` with no number.** The on-request affordance needs a real WhatsApp number or a contact route, and `SHELL-05` forbids inventing contact detail. It is in the message catalogue so it is one edit, not a code change.                                                                                                                                                                                                                                                                                                                                                                                                                             | content      | **needs you** |
| 5.4 | **Concern tiles render a flat sand panel where a photograph belongs.** `docs/04-information-architecture.md` asks for photographic panels, not boxes; the layout is right and the art direction is unproven. Same bound as review item 2.2.                                                                                                                                                                                                                                                                                                                                                                                                                                        | UI           | carried       |
| 5.5 | **Brand names render as type because no logo may be published.** `imageRightsStatus` is `unknown` on all thirteen marks, and decision L-14 makes unknown mean the name renders instead. This is correct today and will look sparse until rights are recorded.                                                                                                                                                                                                                                                                                                                                                                                                                      | legal / UI   | carried       |
| 5.6 | **Country names come from `Intl.DisplayNames`, not the catalogue.** One line instead of three translations per brand, and it stays right when a brand is added — but it is the platform's wording, not ours. If «کرهٔ جنوبی» ever reads wrong for this audience, the override belongs in the catalogue.                                                                                                                                                                                                                                                                                                                                                                            | i18n         | open          |
| 5.7 | **The featured rail is `merchandisingRank` with no editorial control.** There is no way for staff to pin or exclude a product, so "پیشنهاد مؤسسه" is currently whatever the column says. Fine for a dev catalogue; a real merchandising decision before launch.                                                                                                                                                                                                                                                                                                                                                                                                                    | product      | open          |
| 5.8 | **`/shop` has been rendered against the real database, in a browser.** 17 tiles at all three widths, no overflow, no broken imagery, no console errors. What it confirms is narrow, though: the hub renders correctly and is still running on the four hardcoded editorial image paths `R-5` describes. Rendering truthfully and reading from the content spine are different questions, and only the first is answered.                                                                                                                                                                                                                                                           | verification | resolved      |
| 5.9 | **The Shop hub deliberately carries no academy, booking or brand storytelling**, per decision L-1. If the hub feels thin on its own, that is the signal that the Landing is the page that needs building — not that these belong here.                                                                                                                                                                                                                                                                                                                                                                                                                                             | product      | carried       |

### Refinements not worth blocking on

- `SectionHeading`, `ConcernTiles`, `BrandList` and `CategoryLinks` live in one `hub-sections.tsx`. If any of them is wanted by the PLP, it moves out then — not before.
- `OfferLine` and `ProfessionalMark` read the same `OfferState`; the mark is a second position on the tile, not a second truth.
- The hub emits `CollectionPage` and an `ItemList` of concerns rather than of featured products. The products are merchandising that rotates; the PDP describes each properly, with its own real offer.

---

## Design-system adherence and the facet manifest — 2026-08-26

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Kind          | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------- |
| D.1  | **The design system's component library had never been opened.** 228 files, every component with a `.prompt.md` stating its rules. Work proceeded from tokens and the playbook — the foundations — so the tokens were followed and the components were reinvented. Three things shipped wrong because of it: a hand-rolled price control, an invented ornament, and lapis used as the Shop's selected colour. All three fixed; the rule is now in `AGENTS.md` and the inventory in `docs/25-design-system-adherence.md`.                                | process       | resolved      |
| D.2  | **The price filter is now the design system's `Slider`.** Its spec calls it "the only slider in the system", it was already ported and token-bound, and nothing imported it. It stays inside a GET form with hidden inputs carrying the canonical parameter names, so a filtered listing is still addressable and the back button still works.                                                                                                                                                                                                          | drift         | resolved      |
| D.3  | **Teal is now the Shop's accent throughout** — active facets, current sort, current page, product hover. Lapis belongs to My Studio and was reading as the account area. Teal is the one room accent that passes contrast on cool white (10.51:1), which is why it can be an active state on a light listing while firouzeh cannot.                                                                                                                                                                                                                     | drift         | resolved      |
| D.4  | **The `Divider` is in use** — twice on the hub, once on the PLP above the questions. Its own rule is at most twice per page; `Rule` stays the default. Worth checking on screen that it reads as a moment rather than as furniture.                                                                                                                                                                                                                                                                                                                     | UI            | **needs you** |
| D.5  | **The facet manifest closes the ticket #5 decision** `D-18-3` left open, as `PLP-10` requires. Nine rows in `docs/24-facet-manifest.md`, three of them new and all owned by schema that already existed: `line` (brand ranges), `skin_type`, `phase`. Plus `audience` and the price range.                                                                                                                                                                                                                                                              | structure     | resolved      |
| D.6  | **`line` renders only when the results are one brand.** On the hub it would be thirty ranges from every brand side by side, meaning nothing — the one-product dead end the competitive research warns about. This is the behaviour you asked for from the Storyderm and Forlle'd menus, expressed as a rule rather than a special case.                                                                                                                                                                                                                 | product       | open          |
| D.7  | **Six requested filters were not built, each for a stated reason** — discounts (no promotion model, and the brand brief forbids permanent discount furniture), customer scores (no review model; `D-18-3` forbids `AggregateRating` until real reviews exist), active ingredient (`PLP-10` defers ingredient filters until canonical evidence exists — this is the highest-value one and the most work), "recommended by us", "exclusive", and packages. F-4 records what would unblock each. Worth reading and telling me which to work through first. | product       | **needs you** |
| D.8  | **The FAQ block ships as structure with no content.** `FAQPage` markup emits only for questions actually on the page. A concern page answering «لک چقدر طول می‌کشد تا کم شود؟» in her own words is the strongest ranking asset this site can have, and the content is yours to write.                                                                                                                                                                                                                                                                   | content / SEO | **needs you** |
| D.9  | **Promotional banners below the breadcrumb were refused as furniture and allowed as campaigns**, per `L-6`. A dated campaign with a real end date gets a slot; a permanent strip does not, because on medical-grade product permanent discounting tells patients the price was never real.                                                                                                                                                                                                                                                              | product       | open          |
| D.10 | **The domain is now health care, not only skin care** — DS-5. Every facet is a reference table with slugs and translations, so a new product line is rows rather than migrations. `skin_type` is the one facet whose name assumes skin; when a line arrives that has no skin type, the honest move is a second facet rather than a renamed one.                                                                                                                                                                                                         | structure     | carried       |
| D.12 | **There was no page for browsing everything.** `/shop` is the hub screen, and the query grammar's `hub` scope — which `listProducts` has always handled — mapped to the same path. So the filter rail only ever appeared after a scope had already narrowed the results, which is backwards. `/shop/all` now renders it, and the hub links to it.                                                                                                                                                                                                       | correctness   | resolved      |
| D.13 | **The `skin_type` and `phase` facets were empty because those taxonomies had zero rows** — not a bug in the manifest, the queries or the components. Five skin states and a `daily-care` protocol with four phases are now reference taxonomy, and all ten dev products link to both. A facet with no data and a facet that is broken look identical from outside; check the row count first.                                                                                                                                                           | data          | resolved      |
| D.14 | **Arabic catalogue vocabulary is unreviewed, so `/ar` lists products with no facets.** I added Arabic names, then removed them: `reference-data.test.ts` guards that decision in the name of its own test, and inventing unreviewed content is the mistake I keep flagging elsewhere. Arabic concern, skin-state and phase names are yours to supply if `/ar` should be browsable.                                                                                                                                                                      | content       | **needs you** |
| D.15 | **`resolveScopeTitle` hardcoded Persian «فروشگاه»** for the shop breadcrumb and the hub title — the same leak as `getShopHub`'s metadata, found while adding `/shop/all`. `listProducts` now takes the injected translator too.                                                                                                                                                                                                                                                                                                                         | i18n          | resolved      |
| D.11 | **The design system's `FacetRail` collapses its groups and puts a search box inside any facet over ~10 values.** Ours does neither yet — with five concerns and three brands there is nothing to collapse or search. It matters the moment the real catalogue lands.                                                                                                                                                                                                                                                                                    | UI            | open          |

---

## Packet 7 — PLP and search

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Kind         | Status   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------- |
| 7.1  | **Facet counts follow PLP-03: a group is counted with its own selections removed.** Count `brand` with the brand filter still applied and every unselected brand reads zero, so the rail can only ever narrow and becomes a dead end that has to be cleared and restarted. Other groups' selections still apply, or the numbers would be lies. Four integration tests assert both halves.                                                                                                                                                                         | technical    | resolved |
| 7.2  | **The scope's own axis is not offered as a facet.** On `/shop/concern/lak` there is no concern group, because filtering a concern page by a different concern is a question the page should not ask. Worth confirming you agree — the alternative is offering it and letting a customer switch concerns from inside one.                                                                                                                                                                                                                                          | product      | open     |
| 7.3  | **Every facet value is a link, not a checkbox.** A filtered listing has a real address: shareable, refreshable, back-button-able, and rankable. This is the mechanism behind the competitive argument — the dominant Iranian vendor's filters cannot be linked to. It also means the whole rail works with JavaScript disabled.                                                                                                                                                                                                                                   | technical    | resolved |
| 7.4  | **Sort is a chip row, not a dropdown**, per the design playbook. Four options fit on a line; a dropdown hides three of them and opens a native picker over the results on a phone.                                                                                                                                                                                                                                                                                                                                                                                | UI           | carried  |
| 7.5  | **The four listing routes share one outcome switch** in `listing-route.tsx` rather than repeating it. Four copies is four places for one of them to stop handling `invalid-query`.                                                                                                                                                                                                                                                                                                                                                                                | structure    | resolved |
| 7.6  | **The locale-prefix guard caught a second real defect.** `listing-route.tsx` used `redirect` from raw `next/navigation`, which would have sent an English reader from a non-canonical URL to the Persian route. Found before it ran.                                                                                                                                                                                                                                                                                                                              | correctness  | resolved |
| 7.7  | **`invalid-query` renders not-found rather than dropping the bad parameter.** A recognised parameter with an unusable value — `?sort=cheapest` — is a mistake worth surfacing; silently dropping it shows a listing the customer did not ask for and did not notice. Revisit if real traffic shows people hand-editing URLs.                                                                                                                                                                                                                                      | product      | open     |
| 7.8  | **The facet rail sits below the results on mobile, not behind a drawer.** A drawer hides the site's main competitive advantage behind a tap. It costs a scroll on a phone; if that proves worse in practice, the sheet primitive is already installed.                                                                                                                                                                                                                                                                                                            | UI           | open     |
| 7.9  | **Listing `ItemList` numbers items across the whole listing**, so page three starts at 25. Numbering each page from 1 tells a crawler every page holds the same first item. It carries names and URLs only — no price, no availability, which the product page owns with its real offer state.                                                                                                                                                                                                                                                                    | SEO          | resolved |
| 7.10 | **`getShopHub` and `listProducts` now run several queries each** — three concern spotlights on the hub, up to three facet groups on a listing. All bounded and documented, all trivial at ten products. Re-check with a real catalogue; `docs/evidence/c3-trgm-search.md` already sets the precedent for measuring rather than guessing.                                                                                                                                                                                                                          | performance  | open     |
| 7.11 | **Every listing has now been rendered against the real database, in a browser.** At 390, 768 and 1440: the hub shows 17 tiles, `/shop/all` 24, and both the search PLP and the concern, category and brand scopes resolve. No horizontal overflow at any width, no console errors, no broken images, and no screen re-adds `ms-14` over the shell's offset (`E-6`). Two findings came out of it — `7.12` and `7.13`.                                                                                                                                              | verification | resolved |
| 7.12 | **Fixed, and the cause was not where anyone was looking.** Not the routes — `(storefront)/loading.tsx`. A segment-level loading file wraps the segment in Suspense, so the response streams and the status line goes out with the first flush, before the page has decided anything. `notFound()` then had no status left to set. Removing it turns all four cases into real `404`s and leaves real pages at `200`. Gated by `src/lib/navigation/route-status.test.ts`, because a status code is invisible to typecheck, ESLint and every render test. See `R-9`. | SEO          | resolved |
| 7.13 | **Also fixed by removing the loading boundary, and the same cause.** The streamed document carried the route's own metadata and the not-found page's, so both `noindex` directives were flushed. A single unstreamed render emits one. Two symptoms, one mechanism — which is why it was worth finding the mechanism.                                                                                                                                                                                                                                             | SEO / drift  | resolved |

---

## Packet 8 — the product page — 2026-08-26

**Backfilled 2026-08-26, after the fact.** The work landed in `f8fc15d` with a
commit message carrying its full reasoning and **no documentation at all** —
see `8.10`. These entries are reconstructed from the diff and the message,
which is a worse process than writing them at the time and is exactly why the
rule exists.

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Kind        | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------------- |
| 8.1  | **Every product tile had linked to a route that did not exist since packet 3.** `getProduct` was written then and never given a page, so all forty-eight visible products 404'd — and `/shop/all`'s `ItemList` published those same URLs to Google. Closed by the route existing                                                                                                                                                                                                                             | correctness | resolved      |
| 8.2  | **Composition came from the design system's own `ui_kits/storefront/Product.jsx`**, read through the Claude Design project rather than guessed at — a 60/40 editorial split, the disclosure accordion paired with a consultation panel, companions on a surface band. `OfferLine`, `PairsWith` and the token-bound accordion were reused rather than rebuilt, so a tile cannot promise what the page it links to refuses                                                                                     | structure   | resolved      |
| 8.3  | **`object-contain`, not the design system's `cover`.** The DS previews are square; the real packshots are 1916 × 3547, so `cover` in a 4:5 box crops a third off the bottle. That is `R-4`, already raised on the listing tile. Repeating a known defect on a new surface because a component file says so is not adherence — worth confirming you agree, since it is a deliberate divergence from the handoff library                                                                                       | UI          | open          |
| 8.4  | **The authenticity section renders empty rather than promising an IRC code.** The DS component fills «اصالت کالا» with «به‌زودی درج می‌شود». All fifty products carry a null `irc_code`, so shipping that would put a commitment about your business, on a timeline nobody agreed, on every product page. The section is built and appears the moment a real code exists                                                                                                                                     | content     | **needs you** |
| 8.5  | **137 `product_pair` rows are invented, and marked so.** Every row is `source: "development"` — `C-1`, truth per field — so one predicate finds all of them when the real ones arrive. The rule is deliberately mechanical: same Storyderm range, in routine order, bounded to three. That two products share a range is a fact the manifest states; that two products suit a particular face is **advice**, and a seeder is not entitled to invent advice. Which products actually belong together is yours | product     | **needs you** |
| 8.6  | **`resolveEnquiryHref` now fails closed, which changes what `5.3` looks like.** `shop.enquiryHref` is `https://wa.me/` with no number, which opens WhatsApp with no conversation in it. `PDP-09` refuses a fake contact action, so the control disappears while the offer state still renders and explains itself. The gap stays visible as a missing button rather than as a broken one — on the PDP, the PLP and the hub at once. It closes when you supply the number                                     | product     | **needs you** |
| 8.7  | **`Product` structured data emits an `offers` block only when the page shows a price** — never for `professional_only` or `on_request`, where publishing a trade price to Google would be both a fabrication and a commercial leak. The amount is rials under `IRR`: the page renders toman because that is what a customer reads, but toman has no ISO code and quoting it as IRR would understate every price tenfold                                                                                      | SEO         | resolved      |
| 8.8  | **Three gates bit and were right**: the schema contract wanted `product_pair` declared, the locale-prefix guard wanted the new route's raw `notFound` allowlisted, and the integration suite caught `@/lib/site` being imported into `structured-data.ts` — pulling the Next runtime into a module whose own contract says it stays pure. The origin is injected now                                                                                                                                         | correctness | resolved      |
| 8.9  | **Brand and footer links raised above the WCAG 2.2 AA 24 × 24 floor (2.5.8)**, found at 390 in the browser pass. The larger 44px question is still open and still yours — it pulls against `R-3`'s request for _less_ air and is one number for the whole site (`6c.5`)                                                                                                                                                                                                                                      | a11y        | resolved      |
| 8.10 | **This packet shipped with no documentation, and that is the finding.** `f8fc15d` touches twenty-five files and not one `.md`. The ledger's own _Closing a packet_ rule says a packet closes when its row reads done **and** its review-log section exists; the reasoning lived only in a commit message, so `8.4`, `8.5` and `8.6` — three things that need you — never reached the generated register, and the register understated what was waiting on you by three items. Backfilled here                | process     | resolved      |

---

## Packet 11 — density — 2026-08-26

`R-3`. Decisions are `D-1`…`D-5` in [`33-density-decisions.md`](33-density-decisions.md).

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Kind         | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------- |
| 11.1 | **The first pass changed spacing, and the complaint was about size.** `D-2` retuned every gap on the shop surfaces and the maintainer's answer was that the media were still "crazy huge". He was right: an image box is `aspect-ratio` × column width, and neither is spacing. `D-5` caps the height instead. Recording the miss because the lesson generalises — "too spacious" and "too big" are different measurements and the first does not imply the second. | design       | resolved      |
| 11.2 | **The 4:5 ratio was inherited without the canvas it assumed.** The design system's own compact study uses 4:5 at three columns on a **900px** canvas — ~280 × 350. The application uses the same ratio at three columns on ~1200px, which is 380 × 475, and 570 × 712 on the spotlight's split. Copying a ratio out of a study without its width is how a component looks right in the system and wrong in the product.                                             | design       | resolved      |
| 11.3 | **22rem is a judgement, not a measurement.** It is the first cap that makes the hub, spotlight and rail look proportionate at 1440 while leaving a packshot legible at 390. If it is still too large it is one token, in one place.                                                                                                                                                                                                                                 | design       | **needs you** |
| 11.4 | **Type is deliberately unchanged, and the design system disagrees.** Its compact study also drops `h1` 46 → 32 and `h2` 34 → 20. `R-3` protects the editorial type scale by name, so this packet left it alone — changing both at once would make it impossible to tell which did the work. If the shop still reads as too large after the cap, type is the next lever. `D-3`.                                                                                      | design       | **needs you** |
| 11.5 | **Every tile was emitting a Next image warning, on every page.** `<Image fill>` sat inside a `Link` that was `position: static`, so `fill` resolved against the grandparent — correct by accident, warned about correctly. Fixed by making the link the positioned parent. It was pre-existing and only became visible when the dev console was read properly.                                                                                                      | correctness  | resolved      |
| 11.6 | **The mechanism is Tailwind's own `--spacing`, and that is why it needed no `className` edits.** The shop surfaces read five `var(--space-N)` against seventy-seven raw utilities, so a parallel token set would have moved almost nothing. Every spacing utility compiles to `calc(var(--spacing) * N)` and inherits, so one scoped variable retunes all of them. Worth knowing before anyone proposes a compact utility set.                                      | technical    | resolved      |
| 11.7 | **Not yet verified in a browser.** The disk filled during this packet, which took Postgres and then the Docker daemon down with it. Typecheck, lint, 389 unit tests and the Tailwind class gate all pass; the three-width Persian pass and the visual confirmation of `D-5` are outstanding and are the first thing to run once the database is back.                                                                                                               | verification | open          |

---

## Packets 9 and 10 — the cart and its reservations — 2026-08-26

`COM1`. Packets 9 (cart presentation) and 10 (transactional cart) were built
together rather than in sequence, on the maintainer's instruction to take the
full slice. Decisions are `COM-D3`, `COM-D4` and `CART-01`…`CART-05` in
[`system-design/cart-checkout-payment-fulfilment-and-returns.md`](system-design/cart-checkout-payment-fulfilment-and-returns.md);
the Query and Zustand gate is [`32-tanstack-query-adoption.md`](32-tanstack-query-adoption.md).

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Kind               | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ------------- |
| 9.1  | **The cart works with JavaScript disabled, controls included.** `/cart` renders on the server and its three controls are `<form action={serverAction}>`. Measured with JavaScript genuinely off at 390: quantity 2 → 3 and the total ۸٬۳۶۰٬۰۰۰ → ۱۲٬۵۴۰٬۰۰۰, then remove empties the cart. The drawer is JavaScript-only and always will be — a dialog cannot exist otherwise — which is why the two surfaces have different controls and the same model.                                                                                                                            | a11y / correctness | resolved      |
| 9.2  | **The first design gave one fact two owners, and it took a browser to see it.** `/cart` server-rendered, passed the result in as `initialData`, then subscribed to Query. A form action refreshed the server render while the screen kept reading a cache seeded once at mount, so removing a line changed nothing on screen. Typecheck, lint and every unit test passed. The page is not a Query consumer now; `32` §6 records why.                                                                                                                                                 | drift              | resolved      |
| 9.3  | **The publication rule was written by hand twice, and was wrong both times in the same way.** `isPublished && reviewState === "approved"` looks stricter than the catalogue's predicate and is simply incorrect here, because the whole Storyderm catalogue is `draft` by design (`C-1`). The cart refused every add, then marked every line it did hold as withdrawn — while every product page rendered happily. Both mistakes would have vanished in production, where preview is off and the hand-written rule agrees again. Now one `isVisiblePublication` in `lib/preview.ts`. | drift              | resolved      |
| 9.4  | **Zustand and TanStack Query are installed, and the gate is closed as a record.** Both were pre-approved by `architecture/data-and-state-ownership.md` and neither was installed. The drawer is the contract's own named first consumer. `32` documents keys, stale/retry, invalidation, the absent hydration boundary, what is not a consumer, and confirms nothing server-owned reached the store.                                                                                                                                                                                 | architecture       | resolved      |
| 9.5  | **The guest-to-account merge existed for an hour without being called.** `mergeGuestCartIntoAccount` was written, tested by inspection and wired to nothing — a customer signing in would have silently lost their guest cart. Now invoked on the OTP verify success path, awaited so it cannot race the navigation, and `.catch`-guarded so a merge failure never turns a successful sign-in into an error.                                                                                                                                                                         | correctness        | resolved      |
| 9.6  | **A merge that conflicts has no interface.** `COM-D4` requires an all-or-nothing merge that returns explicit per-line conflicts for the customer to resolve — the service does exactly that and the sign-in path discards the result. Today both carts simply stay intact and the guest cookie is not cleared, which is safe and silent. The screen that lets someone choose is Phase D or E work; recording it so it is not mistaken for done.                                                                                                                                      | product / UX       | open          |
| 9.7  | **The cart entry is now visible in the rail and the bottom bar.** `SHELL-04` held it back because an affordance that cannot act must not appear; it can act now. The bottom bar goes from four items to five, which `N-1`'s own reasoning permits — the number it warns about is six — and at 390 five items are 78px each, comfortably over the target floor. The rail places it last at `order: 70`, after the locale switch, which is the manifest's placement and was left alone: where the cart sits on a rail is a design decision, not a side effect of opening a gate.       | UI                 | **needs you** |
| 9.8  | **No checkout control ships anywhere — not even a disabled one.** `CART-05` refuses a dead button. Both surfaces carry a sentence saying checkout is not open yet instead, which is true and is the only thing that changes when Phase E lands.                                                                                                                                                                                                                                                                                                                                      | product            | resolved      |
| 9.9  | **The 20-minute reservation TTL and the 90-day guest cookie are named constants, not literals.** `COM-D3` requires the TTL be visible in operational documentation rather than buried. The cookie's life is how long the browser keeps the key; the `cart.expiresAt` row remains the authority.                                                                                                                                                                                                                                                                                      | technical          | resolved      |
| 9.10 | **The two-cart oversell test passes with the lock removed; the six-cart one does not.** Verified by deleting `FOR UPDATE` and re-running: a two-way race is too narrow to manifest reliably. The contention test is what actually guards the exit gate, and the simpler one is kept only because it states the rule in a form a reader can check. Written into the test so nobody later trusts the wrong one.                                                                                                                                                                        | verification       | resolved      |
| 9.11 | **My own integration fixtures leaked on failure, which is `R-10` again.** Cleanup sat at the end of a test body, so a failing test left carts and reservations holding stock and every later test in the file failed for the wrong reason. Moved into `beforeEach`, which repairs a crashed run on the next pass. The same lesson, one packet after recording it.                                                                                                                                                                                                                    | process            | resolved      |
| 9.12 | **Migration `0007` closes correction `C5`, and it was a hard prerequisite rather than a tidy-up.** `inventory_reservation.source_cart_item_id` was `NOT NULL` with `ON DELETE restrict`, so a cart line holding a reservation could not be deleted at all — `removeLine` was impossible. Now nullable with `SET NULL` plus a required historical `source_cart_id`, proved live: the line deletes, the reservation survives with its variant and quantity, and deleting the cart is still refused.                                                                                    | technical          | resolved      |
| 9.13 | **Stock is never decremented by a cart operation**, asserted rather than assumed. Adding two of something leaves `inventory.onHand` at five and creates a hold for two — `AGENTS.md` rule 6 and `COM-D3`. Decrement belongs to settlement, which is Phase E.                                                                                                                                                                                                                                                                                                                         | correctness        | resolved      |

---

## Packet 6, closing — the browser pass — 2026-08-26

The pass packet 6's exit gate always required and no previous session could run:
390, 768 and 1440, Persian, in three states each — JavaScript on, JavaScript
off, and `prefers-reduced-motion: reduce` — against the real seeded database.
It was runnable this time because the work moved onto the maintainer's own
machine, where there is a database, a dev server and a browser. Handoff §5 is
now historical; it described Cowork, not here.

Driven by Playwright, which is already a devDependency. The script is a
scratch file rather than a committed suite: `test:e2e` is still a stub with no
config, and standing up an e2e programme is `PDP6`'s job, not a side effect of
closing packet 6.

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Kind           | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------- |
| 6c.1 | **The gate is met, with numbers.** All three widths: no horizontal overflow, `lang=fa dir=rtl`, nine sections, 13 images and none broken. With JavaScript off: `h1`×1, `h2`×10, and **zero** inline `opacity: 0` — content present and nothing hidden by default, which is `M-1`.                                                                                                                                                                                                                                                           | verification   | resolved      |
| 6c.2 | **Parallax was running on phones.** `E-1` permits it above `lg` only. Both call sites carry `hidden lg:block`, which satisfies the _room_ half of that constraint — but `display: none` does not stop a scroll listener, so a phone was still paying for a `requestAnimationFrame` loop per layer, which is the _headroom_ half and the hardware `E-1` was written to protect. Gated in `choreography.ts` instead, because a rule that lives in one caller is a habit and the next caller will not have it. That is `E-6`'s lesson exactly. | UI / motion    | resolved      |
| 6c.3 | **Eight footer links failed the WCAG 2.2 AA target-size floor.** 22px tall at 390 against the 24×24 minimum in 2.5.8, and the "inline in a sentence" exception does not cover a link list. Now 33px. This is a conformance floor, not a taste call, which is why it was fixed rather than recorded.                                                                                                                                                                                                                                         | a11y           | resolved      |
| 6c.4 | **Beat 2 was the one region on the page with no accessible name.** «چرا اینجا» is the beat's name and was marked up as a `p` because it is styled as an overline. The beat was therefore missing from the document outline entirely. It is an `h2` now; the styling is untouched.                                                                                                                                                                                                                                                           | a11y           | resolved      |
| 6c.5 | **Eleven of 25 interactive elements are under 44px at 390**, now that the floor is met — three beat CTAs at 30px and the footer set at 33px. 44px is the number `pdp.md` §8 sets for itself, and reaching it means more vertical air, which pulls directly against `R-3`, where you asked for _less_. One number for the whole site, decided once. Worth settling with `R-3` rather than separately.                                                                                                                                        | UI / a11y      | **needs you** |
| 6c.6 | **Arabic renders two sections against Persian's nine**, English seven. That is `L-4` working — a beat whose content is unapproved is absent, not empty-framed — and not a rendering fault. It is also what a thin locale looks like, and it will stay thin until the Arabic content lands. Related: `D.14`.                                                                                                                                                                                                                                 | content / i18n | carried       |
| 6c.7 | **`/` was serving English to English clients**, including Googlebot. Found by loading the Landing in a browser whose `Accept-Language` was `en-US` and getting an English page at the Persian canonical. next-intl's `localeDetection` defaults to on and nobody had chosen it. Now `false`, recorded as `R-3` in `22-locale-routing-decisions.md`, with `x-default` settled alongside it — which also closes `R.4`.                                                                                                                        | SEO            | resolved      |
| 6c.8 | **The Playwright script is not committed.** It measured what it needed to and its findings are above. Committing an ad-hoc script as if it were a suite would make `test:e2e` look implemented when it is not, and `PDP6` calls for the real thing. Recorded so the next agent knows the evidence came from somewhere and can rebuild it.                                                                                                                                                                                                   | process        | carried       |

---

## Packet 6, second pass — scroll storytelling — 2026-08-26

Triggered by the maintainer seeing the first Landing: _"a normal landing"_ — not
storytelling, not using the assets already in the repository, missing motion
that had been asked for, and copy that read as robotic. Decisions in
[`28-landing-experience-decisions.md`](28-landing-experience-decisions.md).

| #    | What                                                                                                                                                                                                                                                                                                                                                                                  | Kind        | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------- |
| 6b.1 | **Parallax was refused by `L-3` and `LAND-07`; the refusal is withdrawn** at the maintainer's instruction. The reasons it was refused are answered rather than argued with — `translate3d` only, rAF off a passive listener, decorative and `aria-hidden`, space reserved so CLS stays zero, off under reduced motion, desktop only. Pinning, snapping and scroll hijack stay refused | UI / motion | resolved      |
| 6b.2 | **`L-2`'s five fixed beats become eight.** The new ones sit between the originals rather than after them. The rule that stops it becoming a scroll of modules is unchanged and matters more: every beat sits beside a fact that is independently true                                                                                                                                 | product     | resolved      |
| 6b.3 | **Ten testimonials are held, and not for a consent reason.** Nine carry `medical_appearance_claim`, `injectable_reference` or a third-party claim — a customer saying a treatment removed a dent in her forehead is a medical claim the _business_ publishes, and the speaker's permission does not make the business's use of it compliant. Decide them one at a time                | legal       | **needs you** |
| 6b.4 | **The 33 display quotes are my edits of other people's words.** Greetings, sign-offs, emoji and elongated letters removed; their sentence and meaning kept; the full transcription stored beside each one so the edit is visible. They are seeded unpublished, so development shows them and production cannot, until you have read them                                              | content     | **needs you** |
| 6b.5 | **Testimonials are Persian-only on purpose.** Translating someone's testimonial puts words in their mouth in a language they did not speak, so the English row carries no body and the exact-locale read drops the quote on `/en`. The rail is therefore absent in English until real English quotes exist                                                                            | content     | open          |
| 6b.6 | **The rail gap was a defect the repository already had a test for.** `shop-hub.screen.test.tsx` asserted its own `<main>` carried no `ms-14`; scoped to one file, it protected one file, and the next two screens reintroduced it — one written the same day by the same author as the test. The guard now walks every file under `src/`                                              | correctness | resolved      |
| 6b.7 | **The Landing's editorial photography is static paths, not object keys.** `mediaUrl` and `C-7` govern catalogue media, whose address changes when the CDN does. This set is art direction versioned with the code, like the brand glyph, and the guard already allows a static literal for exactly that. Worth confirming the line is drawn where you expect                          | structure   | open          |
| 6b.8 | **The Forlle'd beat links to `/shop`, not `/shop/brand/forlled`.** Forlle'd has no catalogue rows — no usable imagery — so the branded route would 404. It moves when the brand is seeded                                                                                                                                                                                             | product     | open          |
| 6b.9 | **`p01` and the transparent Forlle'd hero are still `Permission not verified`.** They render under the same narrow reasoning the Shop hero already uses. One sentence from the distributor closes it                                                                                                                                                                                  | legal       | **needs you** |

---

## Packet 6 — the Landing — 2026-08-26

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Kind            | Status        |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------- |
| 6.1  | **`LANDING0` was not built, because packet 7B built it.** The Landing reads the same four content tables the PLP does, through the same `resolveBlocks`, with `landing` as the surface. `CONTENT-01`'s _"reads come from PostgreSQL, always"_ is satisfied with no second content store — which is why the spine was built on the PLP first                                                                                                                                                                                                                                                                                                                | structure       | resolved      |
| 6.2  | **The forty-two real testimonials are deliberately not imported.** `CONTENT-02` asked for them as unpublished drafts. Consent is `unknown` on every one, and storing a person's words about their own skin with no consent and no purpose is worse than a thin rail. The preview set is four invented quotes whose attribution says «نمونه» in the row. It makes `CONTENT-03`'s guarantee stronger: no path can publish a real testimonial because none is stored                                                                                                                                                                                          | content / legal | **needs you** |
| 6.3  | **Before/after ships as a tested component with nothing behind it, so the beat is absent.** `LAND-09` wanted placeholder imagery. Hard rule 9 makes before/after default-deny, Iranian advertising rules cover implied medical results, and fabricated result imagery is the one placeholder that is not harmless if it leaks. The component exists so the day consent arrives the work is content, not code                                                                                                                                                                                                                                               | content / legal | **needs you** |
| 6.4  | **The `slash` ornament `LAND-06` lists was not drawn.** The design system already owns the section mark — `Divider`, _"the one piece of ornament the system allows"_ — and drawing a second is exactly what `DS-2` recorded happening once already                                                                                                                                                                                                                                                                                                                                                                                                         | UI              | resolved      |
| 6.5  | **`LAND-06` restricts the ornament to lapis and teal bands** because champagne and gold fail contrast on `--ground`. Applied as: the _pieces_ obey it, and the connecting branch line falls back to the hairline token on light ground, so the spine stays continuous instead of appearing and vanishing per section. Worth confirming that reading                                                                                                                                                                                                                                                                                                        | UI              | open          |
| 6.6  | **Beat 2 renders two credentials and no numbers.** `L-2` wants years of practice and students trained beside them; neither is stated in any document here and both are claims about her business. Carried from `L.4`, and still the shortest path to a fuller beat                                                                                                                                                                                                                                                                                                                                                                                         | content         | **needs you** |
| 6.7  | **The portrait and the three doors stay in the message files, not the content spine.** They are the page's skeleton and its primary navigation, and a missing content row must not be able to take the front door with it. Only the beats `L-10` says may be absent are content-driven                                                                                                                                                                                                                                                                                                                                                                     | structure       | resolved      |
| 6.8  | **Both gates caught something again.** `inset-block-0` compiled to nothing — the exact spelling that left the rail unpositioned in packet 4 — and the locale-prefix guard found a hand-built `/fa/` inside the JSON-LD `SearchAction`, which is the easiest place for one to survive because nobody ever clicks it                                                                                                                                                                                                                                                                                                                                         | correctness     | resolved      |
| 6.9  | **The testimonial rail is Swiper, not the scroll-snap rail `LAND-08` describes.** That plan predates `M-3` making Swiper the storefront's one carousel; building a second rail mechanism would be the drift `AGENTS.md` forbids. Autoplay stays impossible because the wrapper does not import the module                                                                                                                                                                                                                                                                                                                                                  | structure       | resolved      |
| 6.10 | **The bounded petal reveal in `L-5` is still not built.** It is proposed, not adopted, and stays that way until you say so                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | UI / motion     | **needs you** |
| 6.11 | **The Landing is rendered, measured and green.** Against the real seeded database and in Chromium at 390, 768 and 1440 in Persian, in three states each — JavaScript on, JavaScript off, and `prefers-reduced-motion: reduce`. No horizontal overflow at any width, 13 images and none broken, `lang=fa dir=rtl` throughout, and with JavaScript off all eight beats are present with zero inline `opacity: 0`, which is `M-1`. `E-1`'s parallax constraints hold: the layers are inert at 390 and 768, drift at 1440, and are inert at 1440 under reduced motion. `/en` and `/ar` were loaded too — no console errors, no overflow, correct `lang`/`dir`. | verification    | resolved      |

---

## Shop hub and PLP refinement backlog — 2026-08-26

Raised after seeing both pages run against the real catalogue. Full entries,
with the decision each one collides with and a recommended shape, are in
[`27-storefront-refinement-backlog.md`](27-storefront-refinement-backlog.md).
**Nothing here is implemented or scheduled**; it lands after Landing, PDP, cart
and checkout.

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Kind               | Status   |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | -------- |
| R.1  | **Infinite scroll instead of pagination.** Recommended as infinite scroll _over_ the paginated URLs rather than replacing them — the real addresses are the competitive argument in `08-competitive-research.md` and the no-JS path. Back-button restoration is the hard part and belongs with `R.2`                                                                                                                                                                                                                                                                                                                                                                                                                                               | product            | open     |
| R.2  | **Filter changes lose the scroll position.** The direct cost of `7.3`, every facet being a link. `scroll={false}` holds it; the real work is telling a keyboard or screen-reader user that anything happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | UX / a11y          | open     |
| R.3  | **Density — tighter, closer to Ant Design.** A genuine conflict with the playbook's 96px rule and _"if a screen starts looking like an admin dashboard, it has gone wrong"_. Recommended split by surface — editorial Landing, compact shop — implemented as tokens and variants, never per-page overrides                                                                                                                                                                                                                                                                                                                                                                                                                                         | design             | open     |
| R.4  | **The product tile is plain and its packshot is cropped.** The crop has a one-line cause: `object-cover` on `aspect-[4/5]` against 1916 × 3547 packshots. `GalleryBand` already uses `contain`, which is why its images look right. The elegance half is a design pass on the system's own `ProductTile`                                                                                                                                                                                                                                                                                                                                                                                                                                           | UI                 | open     |
| R.4a | **Three.js product spin.** Kept off the PLP for a stated reason rather than a vague one: twenty-four tiles is twenty-four WebGL contexts, which browsers cap. Viable on the PDP behind an interaction, after launch, and only when an asset exists to show                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | UI / perf          | open     |
| R.5  | **The hub still runs on four hardcoded editorial image paths.** The content spine's `shop.hub` surface exists and has nothing seeded against it; `CONTENT-01` already says hub reads come from PostgreSQL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | content            | open     |
| R.6  | **The price filter's Apply button.** It exists because a range has no finite set of links and the `GET` form is the no-JS-correct answer. Recommended: everything live, form retained underneath — mixing instant facets with one button is the only clearly wrong option, and it is where we are                                                                                                                                                                                                                                                                                                                                                                                                                                                  | product            | open     |
| R.7  | **The filter rail should be sticky.** Desktop breakpoint only, because on mobile the rail sits below the results by decision `7.8`; and the inner scroll needs logical properties or it lands on the wrong edge in Persian                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | UI                 | open     |
| R.8  | **The top of the PLP is a stack, not a composition** — each piece added by a different packet. Cheapest win is composing what is already there; the interesting one is a skin-type or routine-step chip row above the grid                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | UI / design        | open     |
| R.9  | **Measured, and the SSR document is complete — dismissed.** The Preview tab was showing the first flush of a streamed response, exactly as the entry predicted. Against the running server: 24 `article` elements survive stripping every `script` from the document, so the tiles are real markup and not flight payload; `BreadcrumbList`, `ItemList` with `numberOfItems` 48, and `FAQPage` all emit. The competitive premise in `08-competitive-research.md` is intact and this was never a defect. **The second half of the entry stays open on purpose:** whether a listing route should carry a `loading.tsx` at all is a preference, it is entangled with Cache Components, and `R-9` says to decide it with them rather than before them. | SEO / verification | resolved |
| R.10 | **The integration suite leaves the development database damaged.** It deletes a Persian translation and a product's media in `beforeAll` to create two states the manifest cannot express, and never restores them — which is why «پودر Ultra Lift» shows an image placeholder and «Ultra Essence Clean» is absent from the Persian listing. Recommended fix is re-seeding at the end of the suite: both seeds are idempotent, and that repairs a crashed run too. Workaround today: `pnpm db:reset && pnpm db:seed demo`                                                                                                                                                                                                                          | technical          | open     |

---

## Packet 7B — catalogue truth and the content spine — 2026-08-26

| #     | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Kind         | Status        |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------- |
| 7B.1  | **The schema corrected the plan, and the correction is the decision.** `C-4` said the demo profile would publish draft products. `product_published_state_check` refuses it — published implies approved — so the options were to approve fifty unverified products or weaken a constraint that is right. Neither: the data stays honest and the _read_ relaxes, through a server-owned draft preview that checks `NODE_ENV` first. `docs/14` P2 had already authorised exactly this; the first draft had not read it closely enough.                                                                                                                                                                                                                         | technical    | resolved      |
| 7B.2  | **Preview relaxes publication, not the active-variant requirement.** That is load-bearing rather than incidental: it is what keeps a held product invisible in both modes, and it is why the seeder marks a held product's variants inactive. Preview means "show unapproved", not "show everything".                                                                                                                                                                                                                                                                                                                                                                                                                                                         | technical    | resolved      |
| 7B.3  | **Fifty products from ninety files, grouped by hand.** Size pairs are one product with a variant ladder; ten frames of one subject are one gallery; a colour that names a formula is its own product; a 1 kg salon pack is a separate professional-only product because `isProfessionalOnly` is a property of the product, not the variant. Worth a read: the rules are in `system-design/catalogue/storyderm-catalogue.md` §2 and every one of them is a judgement you may disagree with.                                                                                                                                                                                                                                                                    | product      | **needs you** |
| 7B.4  | **Persian product names are composed, not translated.** «کرم Clinic-A» — the form word translates because it is a fact about the object, the product name stays Latin because transliterating it invents a spelling the supplier has not chosen. Five names needed a hand-set identity where the rule misfired. All fifty are listed in the manifest's `names.display` for review.                                                                                                                                                                                                                                                                                                                                                                            | product      | **needs you** |
| 7B.5  | **Three files are unresolved and stay that way.** Two Protection packshots carry the Korean suffix 신형 ("new model") with no way to tell which generation they are, and one 72 Capsule Mask small pack does not show its colour, which is its formula. They are counted in the manifest with reasons rather than dropped — `docs/14`'s gate is that no file is silently lost.                                                                                                                                                                                                                                                                                                                                                                                | product      | open          |
| 7B.6  | **Two products are deliberately held back.** `Omso Enzyme Wash` because the filename spells "Omso" while its siblings spell "Osmo" and the manifest cannot tell which is the typo; `Pure Origin Cell` because a 30 ml ampoule and a 5 × 7 ml set share a name and the packshots do not settle whether that is one product or two. Both need a product sheet.                                                                                                                                                                                                                                                                                                                                                                                                  | product      | **needs you** |
| 7B.7  | **Concern, skin-state and phase placement is the one inference in the manifest.** It is assigned from the range name where that is unambiguous and marked `"source": "inference"` where it is not, so a reviewer can see exactly which associations are judgement. It is taxonomy placement, not a claim: a product is _filed under_ redness, it does not _claim to treat_ it.                                                                                                                                                                                                                                                                                                                                                                                | product      | **needs you** |
| 7B.8  | **Every FAQ answer is written by me, in her voice, and cannot publish.** They are seeded `reviewState: draft` with `authorNote: "unreviewed_draft"`, and the schema refuses a published draft. The questions lean operational wherever a useful one exists there — routine order, what clinic-only means, why a price is on request — because those are facts about the shop that a draft can state safely. The five concern sets go further and are the ones needing the closest read.                                                                                                                                                                                                                                                                       | content      | **needs you** |
| 7B.9  | **Image rights are still `unknown` on all ninety packshots.** `docs/14` permits publication only at `approved_supplier` or `brand_owned`. Nothing publishes today, so this blocks production rather than this packet — but it blocks production absolutely. Confirm with the supplier, or say to hold the media out of the seed.                                                                                                                                                                                                                                                                                                                                                                                                                              | legal        | **needs you** |
| 7B.10 | **The originals are copied into `public/media/` as well as derived.** It costs a duplicate of 161 MiB on local disk, gitignored, and it buys a directory whose layout already matches the bucket — so the CDN step is one sync with no key rewriting. It is also what makes `originalObjectKey` a truthful pointer rather than a name for a file nobody has put anywhere.                                                                                                                                                                                                                                                                                                                                                                                     | technical    | resolved      |
| 7B.11 | **Offline asset and data tooling is Python** (`C-18`). `sharp` is not installed and cannot be installed here; Pillow and ImageMagick are. Stated as a rule and applied to both scripts rather than left as a one-off. Re-review if `sharp` becomes installable **and** a second Node-side asset task appears.                                                                                                                                                                                                                                                                                                                                                                                                                                                 | structure    | open          |
| 7B.12 | **The fictional `dev` profile is gone** — `C-6` closed. Its removal was held until a replacement existed, and the guard proved its own worth on the way: the first real seed run failed with _"This database already holds a product the development seed did not create (ultra-a-z-cream)"_, which is exactly what mutually-exclusive profiles are for. `commerce.reads.integration.test.ts` now seeds the real catalogue and derives its expectations from the manifest, so a curation change cannot silently invalidate a test.                                                                                                                                                                                                                            | structure    | resolved      |
| 7B.13 | **The class-compilation gate caught three more dead classes**, mine this time: `border-teal-text`, `text-teal-text`, `text-ink-text`. Only `firouzeh`, `stone` and `gold` have `-text` variants. This is the third packet in which that gate has caught something nothing else could see.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | correctness  | resolved      |
| 7B.14 | **A `Tests` GitHub Actions workflow now runs the integration suite against `postgres:16`.** Without it the suite is written and never run, which is worse than not having one: a green unit run reads as "tested" while the half that touches SQL goes unchecked. It also asserts the seed is idempotent by running it twice.                                                                                                                                                                                                                                                                                                                                                                                                                                 | verification | resolved      |
| 7B.15 | **Proven again, on a clean database, and green.** `db:reset && db:seed demo` then `test:integration`: 4 files, 50 tests, run rather than skipped — which the runbook flags as the failure that reads as a pass. 48 of 50 products list, with the two `C-17` holds (`omso-enzyme-wash`, `pure-origin-cell`) provably absent because they have no active variant, which is the mechanism `C-17` chose precisely so preview cannot relax it.                                                                                                                                                                                                                                                                                                                     | verification | resolved      |
| 7B.17 | **Vitest was reading `.git`.** Files that cannot be deleted on this mount are moved aside into `.git/`, and the runner collected them — a retired suite kept running, and a stale copy of a moved test inflated the count by three with nobody noticing. `vitest.config.mts` now excludes `.git/**`. The honest correction: the "336 green" reported at the end of packet 7B was 321.                                                                                                                                                                                                                                                                                                                                                                         | correctness  | resolved      |
| 7B.18 | **Retired files are stuck inside `.git/_agent-quarantine/`.** The mount refuses deletion, so `dev.ts`, `dev-data.ts` and their tests were moved there rather than removed. They are outside git's index and outside the test runner, so they are inert — but they are still on your disk and only you can delete them.                                                                                                                                                                                                                                                                                                                                                                                                                                        | process      | **needs you** |
| 7B.19 | **Product images were rendering a raw object key as a `src`.** `commerce.reads` read `cardObjectKey`/`detailObjectKey` and passed them straight into `MediaView.src` — item `3.5` recorded that verbatim usage as carried, and it stopped being safe the day `C-8` made keys stop being paths. Every tile and gallery image on the storefront was pointing at a relative path. Fixed with `mediaUrlOrNull`, which returns null rather than throwing on an unusable stored value, because losing an image must degrade the page rather than make stock unbuyable (`LOW-8`). **Nothing static could have caught this** — the read concatenated nothing; it just used the wrong column value. The integration test caught it on its first run against real data. | correctness  | resolved      |
| 7B.20 | **Parameter order was canonical on the way out and ignored on the way in.** `PARAMETER_ORDER`'s own comment says _"two URLs differing only in parameter order are two URLs"_, and `?phase=treat&skin_type=dry` served a page rather than redirecting to the canonical spelling. Canonicality is now decided by comparing the incoming pairs against the pairs the query would emit, which subsumes every earlier ad-hoc rule and is idempotent by construction. Compared as decoded pairs, not encoded strings — one encoder writes a space as `%20` and the other as `+`.                                                                                                                                                                                    | correctness  | resolved      |
| 7B.21 | **The `brand` facet offers one value that matches every result**, because Storyderm is the only brand seeded. A control that cannot narrow anything, above controls that can. Proposed as `F-9` with a recommendation rather than fixed, because the general form of the rule is a judgement call on narrow scopes and the case that triggered it disappears when Forlle'd is seeded.                                                                                                                                                                                                                                                                                                                                                                         | product      | **needs you** |
| 7B.16 | **Uncommitted work appeared in `src/app` and `src/components/layout` during this session** — a favicon set, `brand/logo.tsx`, and edits to the rail, footer and bottom navigation — which I did not make and have deliberately left unstaged. If that was another agent or another window, it is still sitting in the working tree.                                                                                                                                                                                                                                                                                                                                                                                                                           | process      | **needs you** |

---

## Hub blocks and the motion stack — 2026-08-26

| #   | What                                                                                                                                                                                                                                                                                                          | Kind         | Status        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------- |
| C.1 | **B1, B3 and B10 are built.** The paginated product spotlight, the concern→product bridge, and the asymmetric photo mosaic. The mosaic is the first use of the cleared Pexels/Unsplash photography. Worth judging together: the hub is now nine bands, and the question is whether that is rich or long.      | UI           | **needs you** |
| C.2 | **Swiper replaced the hand-rolled rail rather than joining it.** Both worked; two would have been drift. `Autoplay` is never imported so it cannot be enabled by a prop, and a test asserts the slides, the product name and its price are all in the server-rendered HTML.                                   | technical    | resolved      |
| C.3 | **anime.js is bounded to choreography.** CSS keeps every state change; anime gets sequences and SVG drawing. The boundary is one sentence in M-4 and it is the thing to hold — the failure mode is anime quietly becoming a second way to do hover.                                                           | technical    | open          |
| C.4 | **`getShopHub` now runs three extra queries**, one per spotlit concern, capped at three concerns and three products. Readable beats clever at this size, and the cap is what makes the cost visible if it ever grows. Re-check when the real catalogue is bigger than ten products.                           | performance  | open          |
| C.5 | **The class-compilation gate flagged `no-js-scroll`**, a class `globals.css` declares to style Swiper's internals. The gate now reads the project's own stylesheets rather than keeping a hand-maintained allow-list that would drift.                                                                        | process      | resolved      |
| C.6 | **The mosaic photography shows other brands' products** — the plaster shot is a French jar with visible text. It reads as texture at that size, but it is not our stock and should not sit next to a buy action. It is placed in an editorial band on purpose; if it ever reads as merchandising, replace it. | UI / content | open          |
| C.7 | **Nine bands is a judgement, not a measurement.** Hero, concerns, spotlight, concern bridges, authenticity, mosaic, brands, categories, closing. If it feels long, the mosaic and the categories are the two that cut cleanest.                                                                               | UI           | **needs you** |

---

## Shop hero and asset pass — 2026-08-26

| #   | What                                                                                                                                                                                                                                                                                                                                                                                                                         | Kind      | Status        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------- |
| A.1 | **The hero is now a shop hero, not a second landing hero.** Its subject is the shelf: Forlle'd's cut-out product photography on our own lapis field, on a gold hairline standing in for the stone slab, with the blossom behind it — plus a three-fact strip answering genuine / delivery / ask-first before the fold. Worth judging on screen against the Landing's hero, since the two must not read as the same page.     | UI        | **needs you** |
| A.2 | **The Forlle'd hero photograph is marked `Permission not verified`** in the asset catalog. It renders anyway, because a retailer showing a manufacturer's product photograph is ordinary and Forlle'd representation is the one confirmed relationship — the same reasoning that keeps logos held under L-14, applied to a lower-risk case. One sentence from the distributor closes it.                                     | legal     | **needs you** |
| A.3 | **I was wrong about the asset provenance and corrected it.** My first `public/images/README.md` said the origin of these files was unknown. `designs/asset-library/2026-08-26/catalog.json` records creator, source and licence for all 22: the editorial and botanical sets are Pexels, Unsplash or CC0 and are cleared for commercial use. Only the four Forlle'd-sourced images are unverified. The README now says that. | process   | resolved      |
| A.4 | **Six cleared Japanese photographs are sitting unused** — the airy sakura branch, Ryōan-ji's raked garden, the Nara tea still life, the stone path, the bamboo silhouette. They are on-brand and licensed. They are unused only because no slot has been designed for them; that is Landing work, not hub work.                                                                                                              | UI        | open          |
| A.5 | **`public/images` is 173MB**, most of it the Storyderm product library. It is in git. Before the first deploy, decide whether supplier photography belongs in the repository or in object storage — `docs/14` already plans storage keys for exactly this.                                                                                                                                                                   | technical | open          |

---

## Hub composition and voice pass — 2026-08-25

Raised by the hub reading as flat and the copy as scripted. Decisions in
[`23-motion-and-interaction-decisions.md`](23-motion-and-interaction-decisions.md).

| #   | What                                                                                                                                                                                                                                                                                                                                                                                                | Kind            | Status        |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------- |
| M.1 | **The Persian copy was rewritten to sound like a person.** This is the item most worth your attention: it is her voice and I am approximating it. Read the hub and mark every line that sounds like me rather than like her — that correction is worth more than another pass from me.                                                                                                              | content / voice | **needs you** |
| M.2 | **No carousel library.** `ScrollRail` is native scroll-snap plus drag and arrows, so the rail is a real list in the document before any script runs. Embla via `shadcn add carousel` is the right choice the day something needs looping, coupled rails or programmatic slide state — and the npm registry is unreachable from this session's environments either way, so you would need to run it. | technical       | open          |
| M.3 | **GSAP, anime.js, AOS, Swiper and Three.js were all refused, with the trigger that would change each.** The one left genuinely open is `motion` for shared-element transitions between PLP and PDP, which CSS cannot do — and which does not exist yet.                                                                                                                                             | technical       | open          |
| M.4 | **The hero takes a video and has none.** The shape is built so a video can never cost LCP or SEO: poster-first, video behind, text beside it, hidden below `md`, off under reduced motion. It needs a short silent loop — the corridor or the treatment room is what the brand line actually describes.                                                                                             | content         | **needs you** |
| M.5 | **The authenticity band renders only when the catalogue has something.** A shop with nothing in it asserting its stock is genuine answers a question nobody asked. Worth confirming you agree, because it is the block that answers this category's biggest objection and it is now conditional.                                                                                                    | product         | open          |
| M.6 | **Concern panels are flat sand with a hover wash.** The layout is right and there is still no photography. This is the single change that would do most for how the hub feels — more than any animation.                                                                                                                                                                                            | UI / content    | **needs you** |
| M.7 | **The blossom ornament shipped early.** It was planned for packet 6, but the authenticity band needed it and you asked for it now. Positions are fixed rather than random, because a decoration that differs between server and client is a hydration mismatch. The bounded petal motion from L-5 is still unbuilt and still needs your yes.                                                        | UI              | open          |
| M.8 | **`ScrollRail` hides its scrollbar.** The arrows and the partially visible next item carry the affordance instead. If it reads as not-scrollable on a trackpad, showing a thin styled scrollbar is a one-line change.                                                                                                                                                                               | UI              | open          |
| M.9 | **The hub is rendered against the real database.** Same pass as `5.8`. The composition and voice work hold up at 390, 768 and 1440.                                                                                                                                                                                                                                                                 | verification    | resolved      |

---

## Locale routing pass — 2026-08-25

Raised by a real defect: from `/fa`, the rail's Shop entry navigated to
`/fa/fa/shop`. Decisions in [`22-locale-routing-decisions.md`](22-locale-routing-decisions.md).

| #   | What                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Kind          | Status        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------- |
| R.1 | **Seventeen call sites each built their own locale prefix**, and `Link` added a second one. Typecheck, ESLint and 242 unit tests all passed — the defect existed only where the two mechanisms met. This is the clearest example so far of why "one mechanism per concern" is a correctness rule and not a style preference, and it is worth remembering the next time a second implementation looks harmless.                                                   | drift         | resolved      |
| R.2 | **Persian now has no URL prefix.** `/` is the Persian landing and `/fa` redirects to it. If any Instagram bio, printed card or existing link points at `/fa`, it still works via the redirect — but the canonical is now the bare path, and that is the address worth publishing from here on.                                                                                                                                                                   | SEO / product | **needs you** |
| R.3 | **The sign-out button sent English and Arabic customers to the Persian landing page.** Found by the new guard on its first run, not by a person. Fixed.                                                                                                                                                                                                                                                                                                          | correctness   | resolved      |
| R.4 | **`x-default` is emitted, and it names Persian.** The question was whether it names Persian or a language-negotiated root. Answered by `R-3`: there is no negotiated root, because `localeDetection` was found to be on by default and was 307ing every English client away from `/` — Googlebot included. `localeAlternates` now emits `x-default` on every route through one function, matching the canonical. Resolved with packet 6, as this entry expected. | SEO           | resolved      |
| R.5 | **`/en` and `/ar` have now been fetched in a browser.** Both return `200` with the right `lang` and `dir`, no console errors, no horizontal overflow and no broken images. Arabic renders two sections against Persian's nine, which is `L-4` behaving correctly — a beat whose content is unapproved is absent, not empty-framed — and not a rendering fault. Related: `D.14`, Arabic catalogue vocabulary.                                                     | verification  | resolved      |

---

## Landing direction pass — 2026-08-25, ahead of packet 6

Raised by reconciling the maintainer's description of the first page against the
IA, the design playbook, and the three content batches. Decisions are in
[`21-landing-composition-decisions.md`](21-landing-composition-decisions.md);
what needs the maintainer is here.

| #    | What                                                                                                                                                                                                                                                                                                                                                                                                          | Kind              | Status        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------- |
| L.1  | **Academy prices are unconfirmed.** `content/academy/` records ۱۸م / ۳۹م / ۶م تومان as `needs_owner_confirmation`, and certificate issuers are unverified. This blocks the Landing's academy beat and the entire `/academy` room, and it is the single highest-leverage answer available.                                                                                                                     | content           | **needs you** |
| L.2  | **No testimonial may be published.** All 42 records carry `publicationConsent = unknown`. Consent has to be collected from named women about their own skin; it is not a flag anyone can set on their behalf. Beat 4's rail will render zero items until it exists.                                                                                                                                           | content / consent | **needs you** |
| L.3  | **Brand relationships and image rights are unresolved.** 13 marks, `imageRightsStatus = unknown` on all of them, official-representative status confirmed for Forlle'd only. No brand logo may be published; the Shop hub's brand row runs on seeded fiction until this lands.                                                                                                                                | content / legal   | **needs you** |
| L.4  | **Beat 2 needs two numbers** — years in practice and students trained. They are claims about the business and cannot be invented. Until supplied, the claim beat renders the two verifiable credentials only.                                                                                                                                                                                                 | content           | **needs you** |
| L.5  | **The blossom petal reveal is proposed, not adopted.** Form and colour treatment are decided (L-5); the bounded six-petal reveal-once needs a yes before it is built. Refusing it costs the page nothing structurally.                                                                                                                                                                                        | UI / motion       | **needs you** |
| L.6  | **Parallax, autoplay carousels and looping testimonial rails were asked for and refused**, against `10-design-playbook.md` Step 5 and the testimonials README. Substitutes are sticky band pinning and a reader-paced RTL scroll-snap rail. Worth a look on screen before assuming the substitute delivers the feeling that was wanted.                                                                       | UI / motion       | open          |
| L.7  | **`LocalBusiness` JSON-LD needs the same three facts as the footer** — address, telephone, opening hours. Review item 4.2 now blocks structured data as well as the footer.                                                                                                                                                                                                                                   | SEO / content     | **needs you** |
| L.8  | **The content review surface is deliberately not an admin.** AUTH4 is out of the block, so the plan is a generated review sheet per batch that writes answers back into the JSON with importers refusing anything still unknown. If that proves awkward in practice, the alternative is bringing a minimal admin forward — which inverts the block's sequence and should be a considered choice, not a drift. | process           | open          |
| L.11 | **The ornament set is drawn in-repo rather than sourced.** Third-party sakura SVGs are mostly CC-BY or non-commercial, and shipping an unlicensed asset on a site whose pitch is that its Japanese products are genuine is a bad trade. If Forlle'd ever supplies official assets with a written usage right, they are quoted as theirs instead.                                                              | legal / UI        | carried       |
| L.12 | **The Japanese concepts are carried by a growth spine, not a values row.** This is the judgement most worth looking at on screen: a branch advancing through five beats is either the coherent storytelling that was wanted, or it is subtler than intended. Cheap to strengthen, cheap to remove.                                                                                                            | UI                | open          |
| L.13 | **3D/WebGL is deferred off the storefront path.** ~600KB before assets against a mobile-first Iranian audience, plus a second motion mechanism. A lazy-loaded brand-story route is the place for it, Phase 5 at the earliest.                                                                                                                                                                                 | technical         | carried       |
| L.14 | **Source batches are completed with fictional development values.** Every invented field is marked in the row itself so a later reader cannot mistake it for a transcription — but this is the judgement with the longest tail. When real content lands, the fictional completion for that batch should be deleted, not left beside it.                                                                       | data              | open          |
| L.15 | **Brand logos need one line each recording where the right comes from.** Twelve of the thirteen marks are `works_with` candidates; "we stock it" is not a licence. Forlle'd's exclusive representation plausibly is.                                                                                                                                                                                          | legal             | **needs you** |
| L.16 | **Before/after carries a second gate beyond consent.** Implied medical-results claims touch Iranian advertising regulation and eNamad, and this repository has no research on either. It belongs beside terms/privacy/returns as a long-lead legal item.                                                                                                                                                      | legal             | **needs you** |
| L.9  | **The 日本製 authenticity mark is a product idea, parked.** Counterfeit anxiety is the category's biggest objection and `اصالت` is already a PDP accordion section. Where an authenticity mark appears and what it asserts belongs with packet 8, not with landing ornament.                                                                                                                                  | product           | carried       |
| L.10 | **`/about`, `/results` and `/academy` do not exist**, so three of the Landing's five beats have no deeper destination. The beats terminate at the rooms that do exist and the deeper links are absent rather than dead.                                                                                                                                                                                       | structure         | carried       |
